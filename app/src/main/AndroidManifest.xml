<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Required for Android 12+ (API 31+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- Required for legacy support (Android 11 and below) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".GHogHunterApp"
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-8354869049759576~9932277958" />
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />

        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.GroundhogActivity">
        </activity>

        <activity
            android:name=".view.TwoPlayerActivity">
        </activity>

        <activity
            android:name=".view.SettingActivity"
            android:label="@string/settingString"
            android:theme="@style/AppSettingTransparent">
        </activity>

        <activity
            android:name=".view.bluetooth.BtCreateGameActivity">
        </activity>

        <activity
            android:name=".view.CreateGameActivity">
        </activity>

        <activity
            android:name=".view.bluetooth.BtJoinGameActivity">
        </activity>

        <activity
            android:name=".view.JoinGameActivity">
        </activity>

        <activity
            android:name=".view.Top10ScoreActivity"
            android:theme="@style/AppThemeForTop10">
        </activity>

        <activity
            android:name=".view.HostGameActivity">
        </activity>
        <activity
            android:name=".view.bluetooth.BtHostGameActivity">
        </activity>
        <activity
            android:name=".view.ClientGameActivity">
        </activity>
        <activity
            android:name=".view.bluetooth.BtClientGameActivity">
        </activity>

        <activity
            android:name=".view.TwoPlayerResultActivity"
            android:label="@string/twoPlayerString"
            android:theme="@style/AppThemeTransparent" />

        <service
            android:name=".services.LocalTop10Service"
            android:enabled="true"
            android:exported="false">
        </service>
        <service
            android:name=".services.GlobalTop10Service"
            android:enabled="true"
            android:exported="false">
        </service>

    </application>

</manifest>